**Release validation — 2026-09-06**

Passed on this physical Linux host:

- Built current CLI and Desktop with `cargo build --workspace --all-features`.
- `hardware-self-test`: `None: pass, served by Tpm`.
- Created three isolated, password-plus-TPM vaults through the current CLI. Status reported the `tpm` backend. Used private D-Bus sessions and synthetic data exclusively.
- Stored and recovered an exact-byte value containing NUL and newline.
- Imported three personal secrets titled `A`, `A`, `A (2)`; repeat import kept all three existing items. JSON export retained all three distinct passwords.
- Exported a native v2 backup and restored it into empty and populated vaults. Verified the project secret and unrelated destination secret.
- Generated an encrypted v1 keyring backup with the production encoder from the pre-fix commit `c9ca725f19ba40aceab495a041a101108c9fc1c6`. Imported it into both destinations: first import added one logical item, repeat import kept it, replacement replaced it. Subsequent writes and exports succeeded.
- Explicitly sealed and then re-unsealed both destination vaults with the native TPM. Recovered the exact-byte project value and exported restored keyring data again. The test build had changed the CLI executable identity after vault creation: reads were correctly denied until the supported password-authenticated `grant-cli` upgrade flow reauthorized the new binary.
- Permission listing succeeded through the native CLI.
- Desktop opened against a separate empty directory and rendered its creation form, password/confirmation inputs, Settings, and Create vault button. Closed the test process afterward.
- Added a permanent pre-fix archive fixture and regression for metadata/value preservation, both conflict policies, populated destinations, next write, and adapter reload. The new test passed. The earlier complete workspace run passed 328 tests; this additional test brings the suite to 329, without claiming a new complete-suite run.
- Clippy with warnings denied and diff whitespace checks passed after the new test.

Remaining release checks:

- Interactive Desktop creation/unlock, import/export, and approval flows. Niri exposes no usable automation input backend here; AT-SPI setup reported no installed schemas/service and the desktop portal advertises no keyboard input. Visual startup alone is not an end-to-end GUI pass.
- Real lock, suspend, logout, and shutdown lifecycle acceptance. These disruptive session actions were not run.
- Native Windows/macOS builds, signing, and hardware acceptance.
- A user-owned backup recovery drill, if desired. The v1 fixture was generated by actual old code using synthetic records; no personal backup was accessed.

This is partial release acceptance, not approval to publish. Service, CLI, and Desktop must ship together for native protocol v12; v2 archive readers retain v1 compatibility.

Evidence: `release-drill.txt`, `release-reopen.txt`, `release-hardware.txt`, `release-v1-regression.txt`, and `release-clippy.txt`. The reproducible v1 fixture and its generator are in `tests/fixtures/`. The local native drill script is saved as `release-drill.py`; it expects the repository's built CLI and `/tmp/factorseal-release-v1.factorseal` (copy the fixture there before running) and should run under a private D-Bus session as recorded in the remediation note.
